#!/usr/bin/env sh

# --------------------------------------------------
# Pre-commit hook.
#
# Runs fast checks on staged files before a commit
# is created to catch common issues before they reach CI.
# Failures here block the commit.
#
# To skip this hook in exceptional cases:
#
# git commit --no-verify
# --------------------------------------------------

npx lint-staged
