#!/usr/bin/env sh

# --------------------------------------------------
# Pre-commit hook.
#
# Runs fast checks before a commit is created to
# catch common issues before they reach CI.
# Failures here block the commit.
#
# To skip this hook in exceptional cases:
#
# git commit --no-verify
# --------------------------------------------------

npx lint-staged
